#!/usr/bin/env bash

# Create data directory
mkdir -p /var/roost/data
chown -R www-data:www-data /var/roost

# Add the www-data user to the dialout group so it can read the serial device
usermod -a -G dialout www-data
